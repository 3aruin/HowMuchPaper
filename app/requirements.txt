Flask==2.0.2  # For framework for building complex web applications
wolframalpha==5.0.0  # For python client built against the `Wolfram|Alpha <http://wolframalpha.com>`_ v2.0 API.
watchdog==2.1.6  # For filesystem events monitoring and to auto-reload
