Flask==3.0.3  # For framework for building web applications
wolframalpha==5.0.0  # For python client built against the Wolfram|Alpha v2.0 API.
watchdog==2.1.6  # For filesystem events monitoring and to auto-reload
